import { HorizontalScrollDirectiveDirective } from './horizontal-scroll-directive.directive';

describe('HorizontalScrollDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HorizontalScrollDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
